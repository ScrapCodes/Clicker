<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:key="multiple_number"
        android:title="@string/pref_title_count_multiples"
        android:defaultValue="@string/pref_default_count_multiple"
        android:selectAllOnFocus="true"
        android:inputType="numberDecimal"
        android:singleLine="true"
        android:maxLines="1" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="mode_list"
        android:title="@string/pref_title_select_mode"
        android:defaultValue="1"
        android:entries="@array/pref_select_mode_titles"
        android:entryValues="@array/pref_select_mode_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <CheckBoxPreference android:key="@string/fs_button_checkbox"
        android:title="@string/pref_title_fs_button"
        android:defaultValue="false" />

</PreferenceScreen>
